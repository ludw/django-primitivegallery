{% load primitivegallery_extras %}
{
    "images": [
    {% for item in items %}
        "{{ item|imageurl:size }}"{% if not forloop.last %},{% endif %}
    {% endfor %}
    ],
    "image": "{{ item|imageurl:size }}"
}
